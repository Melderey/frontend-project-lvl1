name: Eslint-chek

# Run this workflow every time a new commit pushed to your repository
on: push

jobs:
  Eslint:
    runs-on: ubuntu-latest

    strategy:
      # Node versions list
      matrix:
        node-version: [14.x]
 
    steps:
      - name: Run linter
        # Run Linter
        run: make lint 